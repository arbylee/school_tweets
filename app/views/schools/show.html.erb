<%= @school.name %>
<ul>
  <% @tweets.each do |tweet| %>
    <li>
      <div>At <%= tweet.created_at.strftime('%B %d, %Y %l:%M%p') %>, <%= tweet.screen_name %> said:</div>
      <div><%= tweet.text %></div>
    </li>
  <% end %>
</ul>
